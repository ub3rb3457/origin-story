import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.graymatterf0rge.app',
  appName: 'origin-story',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
